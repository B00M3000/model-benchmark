# Development / x86 requirements.
#
# ON A JETSON, USE requirements-jetson.txt INSTEAD. This file installs an
# OpenCV wheel that drags in NumPy 2, which breaks JetPack's torch.

fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.5
pyyaml>=6.0
numpy>=1.24
opencv-python-headless>=4.8
pillow>=10.0

# Development / test only
pytest>=8.0
httpx>=0.27
