# Benchmark artefacts: uploads, per-frame records, rendered videos
data/

# Model weights and TensorRT engines are host-specific and large
*.engine
*.onnx
*.pt

.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/

.DS_Store
*.swp
.idea/
.vscode/

# Third-party agent skill, installed locally to run the UI polish pass.
# Deliberately not vendored: `/plugin marketplace add pbakaus/impeccable`
# installs it into your Claude config, not into this repo, and it is ~3.4 MB
# of Apache-2.0 code that has nothing to do with the benchmark itself.
.claude/skills/impeccable/
